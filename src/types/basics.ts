export interface StringMap {
	[ key : string ] : string;
}

export interface NumberMap {
	[ key : string ] : number;
}

export interface BooleanMap {
	[ key : string ] : boolean;
}

export interface AnyMap {
	[ key : string ] : any;
}

export interface StringListMap {
	[ key : string ] : string[];
}
